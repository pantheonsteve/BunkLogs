"""
config/api_router.py — legacy /api/ redirect shim (retired step 6_2).

All callers now use /api/v1/ directly. This module is kept as a stub so
config/urls.py imports continue to resolve.
"""

urlpatterns = []
