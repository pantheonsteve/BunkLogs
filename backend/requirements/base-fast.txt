# Ultra-minimal base requirements for fastest possible builds
# Only absolutely critical packages

python-slugify==8.0.4
argon2-cffi==23.1.0
redis==5.2.1
hiredis==3.1.0
python-dotenv==1.0.1

# Core Django
django==5.0.13
django-environ==0.12.0
django-model-utils==5.0.0

# Minimal auth (no MFA to reduce dependencies)
django-allauth==65.7.0

django-crispy-forms==2.3
crispy-bootstrap5==2024.10
django-redis==5.4.0

# DRF essentials only
djangorestframework==3.15.2
django-cors-headers==4.7.0
drf-spectacular==0.28.0

# JWT without heavy dependencies
dj-rest-auth
pyjwt
djangorestframework-simplejwt

# Lightweight ninja
django-ninja>=1.3.0

# Essential for production
dj-database-url
whitenoise[brotli]

# REMOVED FOR SPEED:
# - pandas (huge compile time)
# - Pillow (C compilation) 
# - fido2/MFA packages
# - qrcode
# - social auth extras
